pub mod elements;
pub mod assembler;
pub mod solver;
pub mod analysis;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
