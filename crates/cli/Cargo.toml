[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]
bajrang-core = { path = "../bajrang-core" }
model = { path = "../model" }
io = { path = "../io" }

clap = { version = "4", features = ["derive"] }
